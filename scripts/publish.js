// Phase 4: Instagram Graph API — publish and schedule carousels
// Called internally by MCP tools, not directly by users

'use strict';

// TODO: implement in Phase 4
// - publishNow(postId): upload carousel container to Instagram, publish immediately
// - schedulePost(postId, isoDatetime): create container, schedule via Instagram native scheduling
// - Uses Spaces CDN URLs as image sources for Instagram Graph API
// - Requires INSTAGRAM_ACCESS_TOKEN and INSTAGRAM_BUSINESS_ACCOUNT_ID from .env
